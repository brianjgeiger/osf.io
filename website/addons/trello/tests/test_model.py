# TODO: Set up tests
# The model is mostly concerned with OAuth settings. Ensure that adding OAuth tokens for user and node work,
# and that various removal schemes remove the token properly. Includes removing the contributor from the
# project who owns the token as well as normal token removal procedures.